# Lightweight imports used directly by vibevoice_api.server but incompletely
# declared by the upstream package. CI installs this file for its import smoke.
fastapi>=0.115,<1
uvicorn>=0.30,<1
prometheus-client>=0.20,<1
pyyaml>=6,<7
